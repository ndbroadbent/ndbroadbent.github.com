:plain
  ---
  layout: default
  title: 404 - page not found
  ---
#content.not_found
  %h1 404
  %h4 The page that you are looking for does not exist.
  %h4 Check out one of these posts if you like:
  .posts-list
    {% for post in site.posts %}
    .post-excerpt
      %h3 <a href="{{ post.url }}">{{ post.title }}</a>
      %h6 {{ post.date | date: "%A, %B %d, %Y" }}
      %p {{ post.excerpt }}
    {% endfor %}

#sidebar

