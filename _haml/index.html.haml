:plain
  ---
  layout: default
  ---
#content
  .posts-list
    {% for post in site.posts limit:5 %}
    .post
      .header
        .title
          %h3 <a href="{{ post.url }}">{{ post.title }}</a>
          %h6 {{ post.date | date: "%A, %B %d, %Y" }}
      .content
        {{ post.content }}
    {% endfor %}

  %p#footnote
    Didn't find what you were looking for? There's more in the <a href="/archive.html">archive</a>.

#sidebar
  %h3 Latest Posts
  %ul
    {% for post in site.posts limit:5 %}
    %li
      <a href="{{ post.url }}">{{ post.title }}</a>
      %br
      {{ post.date | date: "%B %d, %Y"}}
    {% endfor %}

  {% include common_sidebar.html %}

