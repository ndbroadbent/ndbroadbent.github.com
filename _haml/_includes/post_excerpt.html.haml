.post.excerpt
  .header
    .title
      %h3 <a href="{{ post.url }}">{{ post.title }}</a>
      %h6 {{ post.date | date: "%A, %B %d, %Y" }}
      %span.divider #{"|"}
      %h6 tagged with {{ post.tags | join " " }}
  {{ post.excerpt }}

